services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.multistage
    image: spam-api:cache
    ports:
      - "8080:8080"
    environment:
      REDIS_HOST: cache
      CACHE_TTL: "3600"
    depends_on:
      - cache

  cache:
    image: redis:7-alpine
